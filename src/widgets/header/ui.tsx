import Button from "../../shared/ui/button";

export default function Header() {
  return (
    <div>
      <h1>Cliean</h1>
      <Button name="sign in" onClickHandler={() => {}} />
    </div>
  );
}
